import React, { FC } from 'react';
import { DefaultLayout } from 'src/components/Layouts';
import Head from 'src/components/Head';

const CVTemplates: FC = () => (
  <DefaultLayout>
    <Head title="CV Templates" />
  </DefaultLayout>
);

export default CVTemplates;
