import React, { FC } from 'react';
import { DefaultLayout } from 'src/components/Layouts';
import Head from 'src/components/Head';

const CVExamples: FC = () => (
  <DefaultLayout>
    <Head title="CV Examples" />
  </DefaultLayout>
);

export default CVExamples;
