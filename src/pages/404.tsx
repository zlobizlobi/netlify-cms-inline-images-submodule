import React, { FC } from 'react';
import { DefaultLayout } from 'src/components/Layouts';

const NotFoundPage: FC = () => (
  <DefaultLayout>You came to the wrong place. Welcome!</DefaultLayout>
);

export default NotFoundPage;
